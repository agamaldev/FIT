# إعداد مساحة المستخدم (Firebase) — MAKE ME FIT

تمت إضافة نظام حسابات كامل للموقع: تسجيل دخول عبر **Google** و **Microsoft (Outlook)** و **البريد/كلمة المرور**، مع **مساحة شخصية** تشمل: ملف شخصي + **صورة شخصية قابلة للرفع** + تمارين مفضلة + سجل حاسبة السعرات + خطط تغذية محفوظة + **متابع وزن وقياسات برسم بياني** + **متابعة إنجاز التمارين وسلسلة الأيام (streak)** + **منشئ خطة تمارين مخصصة** + **الصيام المتقطع (توصية ذكية + مؤقّت حيّ + سجل)**.

> الكود جاهز بالكامل، لكنه **لن يعمل فعلياً حتى تُكمل الخطوات التالية** (إنشاء مشروع Firebase ووضع المفاتيح). قبل الإعداد، يظهر زر "تسجيل الدخول" في القائمة، وعند الضغط على الدخول تظهر رسالة "لم يتم إعداد Firebase بعد".

---

## ما تمت إضافته

| الملف | الدور |
|------|-------|
| `assets/js/firebase-config.js` | مفاتيح مشروعك (تملؤها أنت) |
| `assets/js/auth.js` | كل منطق المصادقة والبيانات (`window.FITAuth` / `window.FITData`) + حقن واجهة الدخول في القائمة |
| `auth.html` | صفحة تسجيل الدخول/إنشاء حساب (٣ طرق) |
| `profile.html` | المساحة الشخصية (محمية — تحوّل لصفحة الدخول إن لم تكن داخلاً) |
| `fasting.html` | الصيام المتقطع: توصية ذكية + مؤقّت حيّ + سجل (يعمل بدون دخول عبر localStorage) |
| `firestore.rules` | قواعد أمان قاعدة البيانات (كل مستخدم يصل لبياناته فقط) |
| `storage.rules` | قواعد أمان التخزين (صورة كل مستخدم يكتبها صاحبها فقط) |
| الصفحات الـ٧ | أُضيف لها سطر `auth.js` + أزرار المفضلة/الخطة/الحفظ + شريط الإنجاز |

---

## الخطوة ١ — إنشاء مشروع Firebase

1. افتح [console.firebase.google.com](https://console.firebase.google.com) → **Add project**.
2. بعد الإنشاء: **Project settings (⚙️)** → قسم **Your apps** → اضغط أيقونة الويب **`</>`** → سجّل تطبيقاً.
3. ستظهر لك قطعة `firebaseConfig`. انسخ القيم والصقها في `assets/js/firebase-config.js` بدل كلمات `REPLACE_ME`.

## الخطوة ٢ — تفعيل طرق الدخول

في القائمة الجانبية → **Authentication** → **Get started** → تبويب **Sign-in method**، وفعّل:

- **Email/Password** ← فعّله (يدعم تأكيد البريد وإعادة تعيين كلمة المرور تلقائياً).
- **Google** ← فعّله (يكفي اختيار بريد الدعم).
- **Microsoft** ← يحتاج خطوة إضافية على Azure (الخطوة ٥).

## الخطوة ٣ — قاعدة البيانات Firestore

1. القائمة → **Firestore Database** → **Create database** → اختر **Production mode**.
2. بعد الإنشاء → تبويب **Rules** → الصق محتوى ملف `firestore.rules` بالكامل → **Publish**.

## الخطوة ٤ — التخزين Storage (لصور البروفايل)

1. القائمة → **Storage** → **Get started** → أكمل الخطوات (اختر نفس منطقة Firestore).
2. تبويب **Rules** → الصق محتوى ملف `storage.rules` بالكامل → **Publish**.

> بدون هذه الخطوة، كل الميزات تعمل عدا **رفع صورة البروفايل**.

## الخطوة ٥ — دخول Microsoft (Azure / Entra ID)

> اتركها للأخير — هي الخطوة الأكثر تعقيداً. (دخول Google والبريد يعملان بدونها.)

1. افتح [portal.azure.com](https://portal.azure.com) → **App registrations** → **New registration**.
2. في **Redirect URI** اختر نوع **Web** وضع:
   `https://<projectId>.firebaseapp.com/__/auth/handler`
   (استبدل `<projectId>` بمعرّف مشروعك — تجده في authDomain).
3. بعد الإنشاء، من **Certificates & secrets** → أنشئ **client secret** وانسخ قيمته.
4. ارجع إلى Firebase → Authentication → Microsoft → الصق **Application (client) ID** و **client secret**.
5. لدعم حسابات Outlook الشخصية اختر **Supported account types = Any organizational directory and personal Microsoft accounts**.

## الخطوة ٦ — النطاقات المصرّح بها

Firebase → Authentication → **Settings** → **Authorized domains** — أضِف:
- `localhost` (للتجربة المحلية)
- نطاق موقعك بعد النشر (مثل `yoursite.com`)

---

## التشغيل والتجربة

⚠️ **مهم:** الدخول عبر Google/Microsoft **لا يعمل من فتح الملف مباشرة (`file://`)**. شغّل خادماً محلياً:

```bash
# داخل مجلد المشروع
python -m http.server 8000
# ثم افتح:  http://localhost:8000/auth.html
```

### قائمة التحقق
- [ ] كل الصفحات تُظهر زر **"تسجيل الدخول"** عند الخروج.
- [ ] الدخول بـ Google / Microsoft / البريد ينجح، وتظهر صورتك واسمك في القائمة.
- [ ] `profile.html` يحوّلك لصفحة الدخول إن لم تكن داخلاً، ويعرض بياناتك بعد الدخول.
- [ ] حفظ بياناتك في "بياناتي" → يظهر `users/{uid}` في Firestore.
- [ ] قلب أي تمرين في صفحة التمارين → يظهر ضمن "تماريني المفضلة".
- [ ] "حفظ النتيجة" في الحاسبة → يظهر في "سجل حاسبة السعرات".
- [ ] "حفظ هذه الخطة" في التغذية → يظهر في "خطط التغذية المحفوظة".
- [ ] زر (+) على أي تمرين → يظهر في "خطتي المخصصة" (مع ترتيب/جولات/تكرارات).
- [ ] "أنهيت تمرين اليوم" في صفحة التمارين → ترتفع السلسلة وتظهر في "تقدّمك في التمارين".
- [ ] إضافة قياس وزن في الملف الشخصي → يظهر في الجدول وفي الرسم البياني.
- [ ] رفع صورة بزر الكاميرا على الأفتار → تظهر في القائمة والملف (يتطلب الخطوة ٤).
- [ ] صفحة "الصيام": احسب التوصية → يقترح نظاماً؛ ابدأ الصيام → يبدأ المؤقّت؛ يستمر بعد تحديث الصفحة؛ إنهاء → يظهر في "سجل الصيام" وفي بطاقة الملف الشخصي.

---

## ملاحظات

- مفاتيح `firebaseConfig` **عامة بطبيعتها** في تطبيقات الويب وليست سرّاً؛ الحماية تأتي من `firestore.rules` ومن النطاقات المصرّح بها.
- بيانات كل مستخدم معزولة تماماً عن غيره بواسطة قواعد Firestore.
- القائمة (nav) مكررة يدوياً في كل صفحة (تصميم القالب الأصلي)؛ واجهة الدخول تُحقن تلقائياً بالـJS فلا حاجة لتعديلها. لو أضفت صفحة جديدة، أضِف لها فقط:
  `<script type="module" src="assets/js/auth.js"></script>`
