# أدوات وتجارب محلية — لا تُرفع
.playwright-mcp/
.superpowers/

# ---- .NET / ASP.NET Core (server/) ----
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/
[Dd]ebug/
[Rr]elease/
artifacts/
TestResults/
*.received.*

# Runtime/test upload artifacts (avatars are created at runtime/in tests)
server/FitApi/uploads/

# Local-only compose override (host port remap + Google OAuth secrets)
docker-compose.override.yml
